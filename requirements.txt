# ─────────────────────────────────────────────────────────────────────────────
#  Libra Contract Guardian v2.0 — Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
#  These are the DIRECT dependencies the application imports, pinned to the
#  exact versions verified working on the reference machine (June 2026).
#
#  Pinning direct dependencies (rather than a full `pip freeze`) keeps the file
#  reproducible across Windows and macOS while letting pip resolve the
#  transitive sub-dependencies appropriately for each platform.
#
#  Install:   pip install -r requirements.txt
#
#  Note: the application also requires Ollama (https://ollama.com) running
#  locally with a model pulled (e.g. `ollama pull mistral:7b-instruct`) for the
#  AI phrasing layer. The engines fall back to deterministic output if no model
#  is available, so the app still runs without it.
# ─────────────────────────────────────────────────────────────────────────────

# Web application framework (the UI)
streamlit==1.58.0

# Data handling and tables
pandas==3.0.3

# Charts and visualisations
plotly==6.8.0

# PDF reading (imported as `fitz`)
PyMuPDF==1.27.2.3

# Word document reading/writing (imported as `docx`)
python-docx==1.2.0

# PDF generation (evidence pack, opinions)
reportlab==4.5.1

# HTTP requests (Ollama local API, optional web lookups)
requests==2.34.2

# Similarity / vectorisation for the playbook deviation engine
scikit-learn==1.9.0
